server {
    listen 3000;
    server_name localhost;

    location = /healthz {
        access_log off;
        default_type text/plain;
        return 200 'OK';
    }

    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ /index.html;
    }
}
